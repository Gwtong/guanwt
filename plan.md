# plan.md — 开发计划（子 PRD 实现顺序与依赖）

> 开发计划（AI Coding 流程 ⑧）。依据：agents.md §4 顺序、PRD-主.md
> 反向收录引用：PRD-主.md §6 · 关联：tests/、deps/cloudflare.md

## 实现顺序（过程驱动：逐功能点实现 + 自测，见 agents.md §3）
按十步链路的自然数据流向，依赖前者产出后者：

| 序 | 子 PRD | 依赖 | 说明 |
|---|---|---|---|
| 0 | F-01 圈选分组 | groups.json | 最先确定处理对象与 A/B 分组 |
| 1 | F-02 消息接入回放 | F-01 | 回放器驱动，白名单准入 |
| 2 | F-03 过滤与聚合 | F-02 | 噪声过滤 + 会话窗 |
| 3 | F-04 意图识别 | F-03 | 双段判定 + 降级 |
| 4 | F-07a 去重频控 | F-04 | 去重窗口 + 每日频控 |
| 5 | F-07b 艾特矩阵分流 | F-04、F-07a | 四出口路由 |
| 6 | F-05 商品推荐 | F-04 | Mock 推荐接口 |
| 7 | F-06 响应执行 | F-05、F-07b | 三出口 + 排期替换 |
| 8 | F-08 看板与检验 | 全部 | 数据回流 + Welch t 检验 |
| 9 | 部署点检 | 全部 | Cloudflare（见 deps/cloudflare.md） |

## 依赖关系图（简化）
```
F-01 → F-02 → F-03 → F-04 → F-07a → F-07b ┐
                                            ├→ F-06 → F-08 → 部署
                              F-04 → F-05 ──┘
```

## 每个功能点的闭环
- 实现前：tests/ 中已有该 F-xx 的正常/边界/异常用例（验收标准先于代码，agents.md §7）。
- 实现中：逐功能点自测，不一次赌大结果。
- 实现后：全量回归（流程 ⑪）→ 云端部署点检（流程 ⑫）。

## 当前状态（演示版已完成）
上述 0–8 已在 `webapp/` 中全部实现并验证（见各子 PRD §7 模块路径）。本计划作为"文档↔代码"映射的收口，供后续真实环境改造（接真实四系统、上 Cloudflare）按序推进。
