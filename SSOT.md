# SSOT.md — 单一事实来源（Single Source of Truth）

> 本项目所有**易变且会被多处引用的定义**，只在本文件定义一次。其余文件（BRD / PRD / schema / deps / 代码注释）**只引用、不复制**。
> 这是治「改一处散多处、对不上、杂糅」的根本机制，与 `agents.md` §2 双向引用铁律互补。
>
> **变更纪律**：改以下任何定义 → 只改本文件（或标注的源）→ 同步其镜像文件 → 立刻在 `CHANGELOG.md` 记一笔。禁止在子 PRD / 代码里重新硬编码这些值。

---

## 0. 编号体系（权威）

### 0.1 功能点 F-xx（业务定义见 `BRD.md` §4，详细规则见 `PRD/PRD-F0x.md`）

| 编号 | 名称 | 一句话 |
|---|---|---|
| F-01 | 试点群圈选与 A/B 分组 | 圈选 treatment / control 两组，保证可比 |
| F-02 | 群消息实时接入（复用回传） | 接入现有机器人回传消息流，不新建采集 |
| F-03 | 低信噪比过滤与需求信号提取 | 过滤寒暄/斗图/灌水，只留高潜购买意图 |
| F-04 | 基于大模型的意图识别 | 双段判定（相关→解析），高置信才触发 |
| F-05 | 群内内容匹配与商品推荐 | 需求 ↔ 商品池（≈6000）匹配推荐 |
| F-06 | 触发机器人定向响应/推送 | 经推送策略执行，受 ≤12 条/天约束 |
| F-07 | 感知与隐私保护策略 | 仅高置信且必要才响应，否则静默 |
| F-08 | 群点击活跃率看板与显著性检验 | treatment vs control 活跃率 + 显著性 |

### 0.2 业务需求 R-xx（定义见 `PRD/PRD-主.md` §1 总表）

| 编号 | 名称 | 对应功能点 |
|---|---|---|
| R-01 | 试点群圈选与 A/B 分组 | F-01 |
| R-02 | 群消息实时接入（复用回传） | F-02 |
| R-03 | 低信噪比过滤与信号提取 | F-03 |
| R-04 | 基于大模型的意图识别 | F-04 |
| R-05 | 群内内容匹配与商品推荐 | F-05 |
| R-06 | 触发机器人定向响应/推送 | F-06 |
| R-07 | 感知与隐私保护策略 | F-07 |
| R-08 | 群点击活跃率看板与显著性检验 | F-08 |

> R-xx ↔ F-xx 映射的**唯一权威**是 `PRD/PRD-主.md` §1 总表。本表为速查镜像。

---

## 1. 北极星指标口径（权威）

- **名称**：群点击活跃率
- **公式**：群营销资源点击量 / 群人数
- **基线锚点**：2025-09 运营群大类周均 **7.57%**（成长群 <7.57% 且环比升 / 活跃群 >7.57% / 衰退群 <7.57% 且环比降）
- **成功判据**：treatment 组相对 control 组，在 **p<0.05** 统计显著水平上提升
- **护栏指标**：退群率 / 负反馈不显著高于 control；错发率 < 3% 红线；投入产出比（增量订单价值 / 机器人成本 400 元/月/个）> 1

> 业务背景见 `BRD.md` §1.1、§2、§5。

---

## 2. 策略参数当前值（权威 · 来源 `webapp/server/state.js` 的 `config`）

> **唯一运行时真相在 `state.js`**；`schema/config.md` 是它的文档镜像；本表是它的速查镜像。改参数三处同步 + 记 `CHANGELOG.md`。

| 字段 | 当前值 | 含义 | 归属 |
|---|---|---|---|
| `whitelist` | `['G001','G002','G004','G006','G008']` | 仅白名单群进入处理 | F-02 |
| `tau` | `0.80` | F-04 相关性置信阈值，低于则静默 | F-04 |
| `t_silence` | `60`（秒） | F-03 聚合窗静默边界（超则打包） | F-03 |
| `t_max` | `300`（秒） | F-03 聚合窗硬上限 | F-03 |
| `t_lookback` | `600`（秒） | F-03 上下文回溯窗口 | F-03 |
| `n_freq` | `3` | F-07 每群每日非艾特响应上限 | F-07 |
| `dedup_window` | `86400`（秒 = 24h） | F-07 去重窗口 | F-07 |
| `at_switch` | `true` | F-07 艾特矩阵总开关 | F-07 |
| `ai_mode` | `builtin`（直调 DeepSeek）/ `dify` | F-04 AI 判定段选型 | F-04 |
| `deepseek_key` | 空（仅存后端，不回显前端） | F-04 API Key | F-04 |

---

## 3. 十步主链路步骤名（权威 · 来源第二课 §2.2）

| 步骤 | 名称 | 归属功能点 |
|---|---|---|
| ① | 消息接入 | F-02 |
| ② | 前置过滤 | F-03 |
| ③ | 会话聚合窗 | F-03 |
| ④ | BERT 相关性判定 | F-04 |
| ⑤ | DeepSeek 需求解析 | F-04 |
| ⑥ | 需求去重与频控 | F-07 |
| ⑦ | 分流决策（艾特矩阵） | F-07 |
| ⑧ | 商品获取 | F-05 |
| ⑨ | 响应执行（三出口） | F-06 |
| ⑩ | 数据回流 | F-08 |

---

## 4. 五页运营界面（权威 · 来源第二课 §6）

| 页面 | 文件 | 服务的功能点 |
|---|---|---|
| 实时需求收集 | `client/src/components/StreamPage.jsx` | 全链路 ①–⑩ 可视化 |
| 需求池管理 | `client/src/components/DemandPoolPage.jsx` | F-04、F-07 |
| 响应记录 | `client/src/components/ResponseRecordsPage.jsx` | F-06 |
| 策略配置 | `client/src/components/ConfigPage.jsx` | F-07 + AI Key |
| 效果看板 | `client/src/components/DashboardPage.jsx` | F-08 |

---

*本文件是「定义」的单一来源。任何文件需要上述编号 / 指标 / 参数 / 步骤 / 界面时，引用本文件而非重抄。变更见 `CHANGELOG.md`。*
