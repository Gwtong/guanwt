# agents.md — 项目宪法｜JD-Community-QuickResponse

> 本文件是项目的**全局描述文件 / 项目宪法**，先于一切内容产物存在（AI Coding 流程第 ① 步）。
> 任何 Agent 在本项目下工作的**第一动作**必须是：读取 `agents.md` + `BRD.md` + 当前相关锁定文件，再动手。
> 文档依据：《京东社群电商快速响应【第一课】》（腾讯文档 doc_id=cgtAHgnkZmeW）§8–§9。

---

## 0. 这是什么项目（一句话）
在**不动现有四个系统**的前提下，为**个微社群（C 端个人群）**试点「群内需求快速响应」能力，以**提升群点击活跃率（北极星指标）**为唯一业务目标。完整业务背景与范围见 `BRD.md`。

---

## 1. 产物组织（目录与引用关系）

```
jd-community-quick-response/
├── agents.md          # 宪法（本文件）：每次必读
├── BRD.md             # 业务需求，定义 R-01..R-n 与功能点 F-01..F-0n
├── SSOT.md            # 单一事实来源：编号/指标/参数/链路权威定义（改动只改此处）
├── CHANGELOG.md       # 变更日志：每次改动留痕（可追溯、不杂糅）
├── prototype/         # ③ 可交互前端原型（需求确认工具，非开发产物）
├── schema/            # ④ 数据库字段锁定（表/字段名/类型/口径）
├── deps/              # ⑤ 外部依赖锁定（接口/服务/数据源）
├── techstack.md       # ⑥ 技术栈锁定（语言/框架/库/版本，选主流）
├── PRD/
│   ├── PRD-主.md      # 索引：功能点 ↔ BRD 需求 ↔ 子 PRD（双向引用中枢）
│   └── PRD-F0x.md     # 子 PRD：每功能点一份，写清数据流转/处理规则/异常表现
├── plan.md            # ⑧ 开发计划（子 PRD 实现顺序与依赖）
└── tests/             # ⑨ 测试用例（每功能点 正常/边界/异常 三类）
```

引用链方向（哲学三·双向引用）：
```
BRD（R-xx 业务目标）→ PRD/功能清单（F-xx）→ 子 PRD → 模块与文件 → 上线产品
正向：每个功能点标注来自哪条 BRD 需求
反向：每个模块能说出它为哪条需求服务
```

---

## 1.1 已生成文档清单（互锁实账）

> 本项目的"双向引用铁律"不是口号，下方每一份都已**被 `PRD/PRD-主.md` 收录**，且各自头部**反向引用** BRD / agents.md / 上游文件。新增文件必须登记于此，否则视为孤儿文件。

### 宪法与业务（①②③）
- `agents.md`（本文件）— 宪法，每次必读
- `BRD.md` — 业务需求 R-01..R-08 + 功能点 F-01..F-08
- `prototype/` — ③ 可交互前端原型（需求确认工具）

### 三项锁定（④⑤⑥）
- `schema/` — ④ 字段锁定：`group / product / schedule / message / demand / response / config`（7 份，全带双向引用）
- `deps/` — ⑤ 依赖锁定：`deepseek / dify / cloudflare / robot-systems`（4 份）
- `techstack.md` — ⑥ 技术栈锁定（React+Vite / Node+Express）

### 主子 PRD（⑦）
- `PRD/PRD-主.md` — 双向引用中枢（R-xx ↔ F-xx ↔ 子PRD ↔ schema ↔ deps ↔ 模块）
- `PRD/PRD-F0x.md` — 8 份子 PRD（F-01..F-08，各含处理逻辑/异常/验收/引用）

### 计划与测试（⑧⑨）
- `plan.md` — ⑧ 开发计划（实现顺序与依赖）
- `tests/索引.md` — ⑨ 测试用例索引（每 F-xx 正常/边界/异常验收点）

### 单一事实来源与变更日志（护栏）
- `SSOT.md` — 单一事实来源：所有易变定义（编号 F/R、北极星口径、策略参数、十步链路、五页界面）的权威，改动只改此处并记 CHANGELOG
- `CHANGELOG.md` — 变更日志：每次改动记一笔，保证迭代可追溯、不杂糅

### 落地产物（实战版）
- `webapp/` — React+Express 前后端分离应用（localhost:3001），已实现十步链路 + 五页运营界面，模块与子 PRD §7 一一对应

---

## 2. 铁律：拒绝孤儿文件（双向引用准入规则）

> 引用链不是事后台账，而是从第一份产物起强制执行的准入规则。

每产生一份新产物，必须同时满足：
1. **正向引用**：标注它服务哪条 BRD 需求（R-xx）与哪个功能点（F-xx）。
2. **反向引用**：被上层索引收录（如 `PRD-主.md` 收录每个 F-xx，BRD 收录每个 F-xx）。
3. **无孤儿**：任何文件若既不被其他文件引用、也不引用任何其他文件，禁止提交——它是迭代时的定时炸弹。

---

## 3. 三条哲学落地（对治 Agent 三个天性）

| 哲学 | 对治天性 | 在本项目的落地 |
|---|---|---|
| **关键规则** | 概率生成（漂移） | 技术栈 / schema / 目录 / 命名 / 禁区在 ④⑤⑥ 锁死，开工前必读，反复生效 |
| **过程驱动** | 上下文有限 | 十二步顺序不可跳；逐功能点实现 + 自测，把「一次赌大结果」换成「多次锁小结果」 |
| **双向引用** | 无状态（失忆） | R-xx ↔ F-xx ↔ 模块文件 全链路可追溯，引用链即项目外置记忆 |

---

## 4. 十二步顺序（不可跳步）

```
① agents.md（宪法）
   → ② BRD（本版需求 + Out of Scope）
   → ③ 可交互前端原型（钉死需求，定稿即完成使命）
   → ④ 数据库字段  → ⑤ 外部依赖  → ⑥ 技术栈   （三项锁定）
   → ⑦ 主子 PRD    → ⑧ 开发计划   → ⑨ 测试用例
   → ⑩ 逐功能开发自测 → ⑪ 全量回归 → ⑫ 云端部署点检
```

顺序即方法论：**规矩在需求前，需求在约束前，约束在方案前，方案在代码前**。跳步的代价在后面某一步加倍偿还。

---

## 5. 禁区（不许动）

- 现有四个系统逻辑与数据：机器人系统 / SCRM 系统 / 商品池与活动系统 / 推送策略管理系统。
- `BRD.md` 中 **Out of Scope** 列明的范围（含企微社群、全量 23 万群、CPS 分润结算、群主培训）。
- 群主 CPS 分润 / 佣金结算链路。

---

## 6. 命名规范

| 类型 | 格式 | 示例 |
|---|---|---|
| 业务需求 | `R-01` ... | R-01 试点群圈选 |
| 功能点 | `F-01` ... | F-04 意图识别 |
| 子 PRD | `PRD-F0x.md` | PRD-F04.md |
| 测试用例 | `tests/F0x-正常\|边界\|异常.md` | tests/F04-边界.md |
| 数据库字段文件 | `schema/<表名>.md` | schema/group.md |
| 外部依赖文件 | `deps/<依赖名>.md` | deps/robot-callback.md |

---

## 7. 验收标准先于代码

每个 `F-xx` 在 `tests/` 中**先有**正常 / 边界 / 异常三类用例，才允许进入开发（⑩）。
Agent 没有「看起来能跑就算完成」的空间——验收标准先于代码存在。

---

## 8. Agent 工作前必读清单（每轮对话）

1. `agents.md`（本文件）
2. `BRD.md`
2.5 `SSOT.md`（权威定义：改编号/指标/参数前必读）
3. 当前任务相关的锁定文件：`schema/*`、`deps/*`、`techstack.md`
4. `PRD/PRD-主.md` + 当前要做的那份 `PRD/PRD-F0x.md`

> 上下文有限：每次只需装载「主 PRD + 当前子 PRD」，不必装下全部方案。
